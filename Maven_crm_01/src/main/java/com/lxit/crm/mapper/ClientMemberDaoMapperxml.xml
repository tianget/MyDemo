<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.lxit.crm.dao.ClientMemberDao">
 	
	<select id="getAllMember" resultType="Client_member">
	SELECT * FROM client_member WHERE mid=#{m_id}
	</select>
 </mapper>